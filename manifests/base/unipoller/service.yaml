apiVersion: v1
kind: Service
metadata:
  name: unifi-poller
  namespace: unipoller
  labels:
    app: unifi-poller
    type: poller
spec:
  ports:
  - name: http
    port: 9130
    protocol: TCP
    targetPort: 9130
  selector:
    app: unifi-poller
    type: poller
  type: ClusterIP
