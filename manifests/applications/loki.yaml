apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: loki
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-12"
spec:
  project: infrastructure
  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: loki
      targetRevision: 7.1.0
      helm:
        valueFiles:
          - $values/manifests/base/loki/values.yaml
    - repoURL: git@github.com:imcbeth/homelab.git
      path: manifests/base/loki
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: loki
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: "apps"
      kind: "StatefulSet"
      jqPathExpressions:
        # Ignore entire VCT to prevent delete+recreate of Loki StatefulSet
        # when chart upgrades change VCT fields (which orphans the iSCSI LUN on NAS)
        - .spec.volumeClaimTemplates
        - .status
