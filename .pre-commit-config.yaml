# Pre-commit hooks for homelab Kubernetes repository
# See https://pre-commit.com for more information
# Installation: pip install pre-commit && pre-commit install

repos:
  # General file checks
  # git-crypt-encrypted paths (per .gitattributes): secrets/**, *secret*, *.key, *-sealed.yaml.
  # These are binary in CI (no decryption key), so any text-mutating hook MUST skip them.
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
        name: Trim trailing whitespace
        exclude: '(^secrets/|.*secret.*|.*\.key$|.*-sealed\.ya?ml$)'
      - id: end-of-file-fixer
        name: Fix end of files
        exclude: '(^secrets/|.*secret.*|.*\.key$|.*-sealed\.ya?ml$)'
      - id: check-yaml
        name: Check YAML syntax
        exclude: '(^secrets/|.*secret.*|.*\.key$|.*-sealed\.ya?ml$)'
        args: ['--unsafe']  # Allow custom tags in Kubernetes manifests
      - id: check-added-large-files
        name: Check for large files
        args: ['--maxkb=1000']
      - id: check-merge-conflict
        name: Check for merge conflicts
      - id: mixed-line-ending
        name: Check line endings
        args: ['--fix=lf']
        exclude: '(^secrets/|.*secret.*|.*\.key$|.*-sealed\.ya?ml$)'
      - id: detect-private-key
        name: Detect private keys
        exclude: '(^secrets/|.*secret.*|.*\.key$|.*-sealed\.ya?ml$)'

  # YAML linting with Kubernetes-friendly rules
  - repo: https://github.com/adrienverge/yamllint
    rev: v1.35.1
    hooks:
      - id: yamllint
        name: Lint YAML files
        exclude: '(^secrets/|.*secret.*|.*\.key$|.*-sealed\.ya?ml$|^manifests/base/grafana/dashboards/.*\.yaml$)'  # git-crypt encrypted paths + Grafana dashboards
        args:
          - '-d'
          - |
            extends: default
            rules:
              line-length:
                max: 250
              document-start: disable
              truthy:
                check-keys: false
              indentation:
                spaces: 2
                indent-sequences: consistent
              comments:
                min-spaces-from-content: 1

  # Kubernetes manifest validation using kubeconform
  - repo: local
    hooks:
      - id: kubeconform
        name: Validate Kubernetes manifests
        entry: scripts/validate-manifests.sh
        language: system
        pass_filenames: false
        files: '^manifests/.*\.ya?ml$'
        exclude: '^secrets/.*'
        verbose: true
        description: 'Validate Kubernetes manifests against schemas (manifests without available schemas are skipped due to -ignore-missing-schemas)'

      - id: prometheusrule-label
        name: PrometheusRule release label
        entry: scripts/validate-prometheusrules.sh
        language: script
        files: ^manifests/.*\.(yaml|yml)$
        pass_filenames: true

      # Parses YAML/JSON that lives INSIDE ConfigMap data: keys.
      #
      # yamllint and kubeconform validate the OUTER document, where a data:
      # value is just an opaque scalar. Nothing else in this chain ever parses
      # it. On 2026-09-19 a comment inserted at the wrong indentation left the
      # Loki alerting rules unparseable; the ruler ran with ZERO rules loaded
      # and every hook here passed. Grafana dashboards ship the same way.
      - id: configmap-embedded
        name: Validate YAML/JSON inside ConfigMaps
        entry: scripts/validate-configmap-embedded.py
        language: system
        files: '^manifests/.*\.ya?ml$'
        exclude: '(^secrets/|.*secret.*|.*\.key$|.*-sealed\.ya?ml$)'
        pass_filenames: true

      - id: kustomize-build
        name: Validate kustomizations build
        entry: scripts/validate-kustomizations.sh
        language: system
        pass_filenames: false
        files: '^manifests/.*\.ya?ml$'
        exclude: '^secrets/.*'
        verbose: true
        description: 'Run kustomize build on every kustomization.yaml and validate the rendered output with kubeconform. Skips locally if kustomize is not installed; CI always runs it.'

  # Secret scanning to prevent accidental commits
  # Pinned to match the version CI installs in .github/workflows/gitleaks.yml.
  # These MUST stay in step. They drifted once (hook v8.18.1 vs CI v8.30.1) and
  # disagreed on identical input: v8.18.1 flagged AWS's documented sample access
  # key that v8.30.1 allowlists, so a commit could pass CI and fail the hook.
  # When bumping one, bump the other.
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
        name: Detect secrets and credentials
        exclude: '^(secrets/.*|manifests/base/.*/secret\.yaml|manifests/base/.*/.*-secret\.yaml|manifests/base/external-dns/secret-.*\.yaml|.*-sealed\.yaml)'

  # Markdown linting
  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.39.0
    hooks:
      - id: markdownlint
        name: Lint Markdown files
        args: ['--fix', '--disable', 'MD013', 'MD033']
        exclude: '(^secrets/|.*secret.*|.*\.key$|.*-sealed\.ya?ml$)'
        # MD013: Line length
        # MD033: Inline HTML

  # Check for TODO/FIXME in commits (warning only)
  - repo: local
    hooks:
      - id: check-todos
        name: Check for TODO/FIXME markers
        entry: bash
        args:
          - -c
          - |
            files=$(git diff --cached --diff-filter=d --name-only)
            if [ -n "$files" ]; then
              echo "$files" | xargs grep -nHE "TODO|FIXME" 2>/dev/null && echo "Warning: Found TODO/FIXME markers" || true
            fi
            exit 0
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
